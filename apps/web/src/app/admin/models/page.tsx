"use client"; import {useCallback,useState} from "react"; import {AdminView,Empty} from "../admin-view"; import {fetchAdminModels,updateAdminModel} from "@/lib/admin-api"; import {useAuth} from "@/lib/auth-context"; import {Input} from "@/components/ui/input"; import {Button} from "@/components/ui/button";
function ModelEditor({model}:{model:any}){const{session}=useAuth();const[credits,setCredits]=useState(model.credit_price);const[cost,setCost]=useState(model.cost_price_fen/100);const[enabled,setEnabled]=useState(model.enabled);async function save(){await updateAdminModel(session!.access_token,model.model_id,{creditPrice:Number(credits),costPriceFen:Math.round(Number(cost)*100),minimumPlan:model.minimum_plan,enabled})}return <div className="rounded-md border bg-background p-4"><div className="mb-4 flex justify-between"><div><h2 className="font-medium">{model.display_name}</h2><p className="text-xs text-muted-foreground">{model.model_id} · {model.generation_type}</p></div><label className="text-sm"><input type="checkbox" checked={enabled} onChange={e=>setEnabled(e.target.checked)}/> 启用</label></div><div className="grid gap-3 sm:grid-cols-2"><label className="text-sm">每次生成积分<Input type="number" min="1" value={credits} onChange={e=>setCredits(Number(e.target.value))}/></label><label className="text-sm">服务商成本（元）<Input type="number" step="0.01" value={cost} onChange={e=>setCost(Number(e.target.value))}/></label></div><Button className="mt-4" onClick={save}>保存 {model.display_name}</Button></div>}
export default function ModelsPage(){const load=useCallback(fetchAdminModels,[]);return <><h1 className="mb-6 text-xl font-semibold">模型定价</h1><AdminView title="" load={load}>{(d:any)=>d.models.length?<div className="grid gap-4">{d.models.map((m:any)=><ModelEditor key={`${m.model_id}:${m.generation_type}`} model={m}/>)}</div>:<Empty label="模型"/>}</AdminView></>}
